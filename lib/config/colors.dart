import 'package:flutter/material.dart';

const primaryColor = Color(0xff5D3FD3);
MaterialColor mainAppColor = const MaterialColor(
  0xffff1493,
  <int, Color>{
    50: primaryColor,
    100: primaryColor,
    200: primaryColor,
    300: primaryColor,
    400: primaryColor,
    500: primaryColor,
    600: primaryColor,
    700: primaryColor,
    800: primaryColor,
    900: primaryColor,
  },
);

const textInputIconColor = Color(0xffff1493);
const authTextInputHintColor = Color(0xFFbebebe);

const secandaryColor = Color(0xffff1493);
final buttonColor = Colors.pinkAccent.shade200;
const buttonTexrColor = Colors.white;
const inputTexrColor = Color(0xffff1493);

const topTabBackground = Color(0xFFE5E5E5);
const topTabForground = primaryColor;
const dashboardItemBorderColor = Color(0xffff1493);

const dashboardTextColor = Colors.white;
const logoTextColor = Colors.white;

const bgColor = Color(0xffE5E8E8);
const appBarTitleColor = Colors.white;
const titleColor = Colors.pink;
const secondaryTextColor = Colors.black;

const dashbboardTitleColor = Colors.white;

// ////tabs
const tabTextColor = Colors.black;
